header.panel.text-center
  h3 = @round.course_name
  h5 = @round.display_date

= render "subheader"

- @round.scorecards.each do |scorecard|
  .row.js-scorecard-row class=(scorecard.id)
    .small-3.columns.user-display
      h2.text-centered = scorecard.user_initials
    .small-6.columns.scorekeeper
      .row
        .small-5.columns
          = link_to "#", class: 'button expand round js-minus-from-score', rel: 'minus' do
            i.fi-minus
        .small-2.columns
          h2.text-centered.js-hole-score class=(scorecard.class_for_turn(@hole)) data-turn-id="#{scorecard.turn_for_hole(@hole).id}" = scorecard.score_for_hole(@hole)
        .small-5.columns
          = link_to "#", class: 'button expand round js-add-to-score', rel: 'plus' do
            i.fi-plus
    .small-3.columns.text-centered
      h2.left.total = scorecard.total
      h4.left.shooting = scorecard.shooting

= render "footer"
